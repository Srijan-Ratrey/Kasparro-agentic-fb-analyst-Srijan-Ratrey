# Runtime dependencies
pandas>=2.0.0
numpy>=1.24.0
pyyaml>=6.0
scikit-learn>=1.3.0
jsonschema>=4.0.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0

# Development tools
black>=23.0.0
flake8>=6.0.0
mypy>=1.5.0

# Notes:
# - Removed webserver, messaging, plotting, DB migration, and LLM libraries
#   which are mentioned in documentation but not used in the `src/` code.
# - If you plan to enable optional features (FastAPI app, Langfuse, OpenAI
#   integration, plotting, or DB migrations) add those back when the
#   corresponding code is introduced.
